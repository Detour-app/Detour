<?xml version="1.0" encoding="utf-8"?>
<!-- Scoped to the one cache subdirectory GPX exports are written to. Trips and
     traces live in filesDir and are deliberately not reachable from here. -->
<paths>
    <cache-path name="shared" path="shared/" />
</paths>
