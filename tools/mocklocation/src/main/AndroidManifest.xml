<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Grants nothing at runtime — it has been a no-op permission for years.
         It is here because Settings → Developer options → "Select mock location
         app" builds its list from the apps that declare it, so without this the
         harness installs fine and simply never appears in the picker. That
         matters on OEM builds that refuse `appops set … android:mock_location`
         over adb (ColorOS does), where the picker is the only way in. -->
    <uses-permission android:name="android.permission.ACCESS_MOCK_LOCATION"
        tools:ignore="MockLocation" />

    <application
        android:label="Detour Mock Location"
        android:allowBackup="false">

        <!-- Exported so adb can start it directly. Debug-only harness; it is
             never installed on anything but a test device. -->
        <service
            android:name=".MockService"
            android:exported="true"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>
